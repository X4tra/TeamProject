version: '3.8'

services:
    dashboard:
        build: ./dashboard
        container_name: dashboard
        ports:
            - "8000:8000"
        networks:
            - TPnet
        restart: unless-stopped
    # api:
    #     build: ./backend
    #     networks:
    #         - TPnet
    #     restart: unless-stopped
    # counter:
    #     build: ./counter
    #     networks:
    #         - TPnet
    #     restart: unless-stopped
    test:
        build: ./test
        container_name: test
        networks:
            - TPnet
        restart: unless-stopped
        depends_on:
            - dashboard

networks:
    TPnet:
        driver: bridge
