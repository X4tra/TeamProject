version: '3.8'

services:
    dashboard:
        build: ./dashboard
        container_name: dashboard
        networks:
            - TPnet
        restart: unless-stopped
    # api:
    #     build: ./backend
    #     networks:
    #         - TPnet
    #     restart: unless-stopped
    # counter:
    #     build: ./counter
    #     networks:
    #         - TPnet
    #     restart: unless-stopped
    test:
        build: ./test
        container_name: test
        networks:
            - TPnet
        restart: unless-stopped

networks:
    TPnet:
        driver: bridge
